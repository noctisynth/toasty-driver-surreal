name: Quality

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    name: Rust ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
      - name: Format
        run: cargo fmt --all --check
      - name: Check
        run: cargo check --workspace --all-targets --locked
      - name: Clippy
        run: cargo clippy --workspace --all-targets --locked -- -D warnings
      # The three skipped cases are DynamoDB-specific negative assertions that
      # SurrealDB does not share; the driver correctly performs the operation,
      # so they "fail" by succeeding. See tests/mem.rs for the rationale.
      - name: Test (in-memory engine)
        run: >-
          cargo test --workspace --locked --
          --skip composite_index_too_many_range_columns
          --skip composite_unique_index_unsupported_on_dynamodb
          --skip starts_with_empty_prefix
      - name: Documentation
        run: cargo doc --workspace --no-deps --locked
        env:
          RUSTDOCFLAGS: -D warnings

  rocksdb:
    name: RocksDB end-to-end
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
      # The embedded RocksDB engine compiles librocksdb from source, so it runs
      # in its own job. The e2e tests share one on-disk datastore per case and
      # must run single-threaded.
      - name: e2e (kv-rocksdb)
        run: cargo test --test e2e_rocksdb --features rocksdb --locked -- --test-threads=1
